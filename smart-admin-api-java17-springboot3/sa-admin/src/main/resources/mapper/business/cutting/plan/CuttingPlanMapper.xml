<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.cutting.plan.dao.CuttingPlanDao">
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.cutting.plan.domain.vo.CuttingPlanVO">
        SELECT plan_id, plan_no, order_id, order_no, customer_name, style_no, style_name,
               plan_date, plan_quantity, actual_quantity, status, remark, create_user_name, create_time
        FROM t_cutting_plan
        <where>
            deleted_flag = 0
            <if test="queryForm.planNo != null and queryForm.planNo != ''">
                AND INSTR(plan_no, #{queryForm.planNo})
            </if>
            <if test="queryForm.orderNo != null and queryForm.orderNo != ''">
                AND INSTR(order_no, #{queryForm.orderNo})
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
        </where>
        ORDER BY plan_id DESC
    </select>
</mapper>
