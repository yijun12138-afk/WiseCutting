<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.basic.fabric.dao.FabricSkuDao">

    <select id="queryByFabricId" resultType="net.lab1024.sa.admin.module.business.basic.fabric.domain.vo.FabricSkuVO">
        SELECT sku_id, fabric_id, color_name, color_code, sku_no, price, image_url
        FROM t_fabric_sku
        WHERE fabric_id = #{fabricId} AND deleted_flag = 0
    </select>

    <select id="queryByFabricIds" resultType="net.lab1024.sa.admin.module.business.basic.fabric.domain.vo.FabricSkuVO">
        SELECT sku_id, fabric_id, color_name, color_code, sku_no, price, image_url
        FROM t_fabric_sku
        WHERE fabric_id IN
        <foreach collection="fabricIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted_flag = 0
    </select>

    <update id="deleteByFabricId">
        UPDATE t_fabric_sku SET deleted_flag = 1 WHERE fabric_id = #{fabricId}
    </update>

</mapper>
