<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.production.order.dao.ProductionOrderFabricDao">

    <select id="queryByOrderId" resultType="net.lab1024.sa.admin.module.business.production.order.domain.vo.OrderFabricVO">
        SELECT id, order_id, fabric_id, fabric_no, fabric_name, color_name
        FROM t_production_order_fabric
        WHERE order_id = #{orderId} AND deleted_flag = 0
    </select>

    <delete id="deleteByOrderId">
        UPDATE t_production_order_fabric SET deleted_flag = 1 WHERE order_id = #{orderId}
    </delete>

</mapper>
