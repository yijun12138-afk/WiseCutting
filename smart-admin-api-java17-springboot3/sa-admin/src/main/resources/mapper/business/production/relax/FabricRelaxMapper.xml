<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.production.relax.dao.FabricRelaxDao">

    <!-- 分页查询松布任务 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.production.relax.domain.vo.FabricRelaxVO">
        SELECT relax_id, order_id, order_no, fabric_id, fabric_no, fabric_name,
               color_name, require_length, actual_length, relax_hours, status,
               remark, create_user_name, create_time
        FROM t_fabric_relax
        <where>
            deleted_flag = 0
            <if test="queryForm.orderNo != null and queryForm.orderNo != ''">
                AND INSTR(order_no, #{queryForm.orderNo})
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
        </where>
        ORDER BY relax_id DESC
    </select>

</mapper>
