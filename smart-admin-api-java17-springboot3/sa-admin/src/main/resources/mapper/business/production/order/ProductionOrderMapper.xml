<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.production.order.dao.ProductionOrderDao">

    <!-- 分页查询生产指令单 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.production.order.domain.vo.ProductionOrderVO">
        SELECT order_id, order_no, customer_id, customer_name, style_no, style_name,
               delivery_date, unit_price, order_quantity, finish_quantity, status,
               issue_date, remark, create_user_name, create_time
        FROM t_production_order
        <where>
            deleted_flag = 0
            <if test="queryForm.orderNo != null and queryForm.orderNo != ''">
                AND INSTR(order_no, #{queryForm.orderNo})
            </if>
            <if test="queryForm.styleNo != null and queryForm.styleNo != ''">
                AND INSTR(style_no, #{queryForm.styleNo})
            </if>
            <if test="queryForm.status != null">
                AND status = #{queryForm.status}
            </if>
        </where>
        ORDER BY order_id DESC
    </select>

</mapper>
