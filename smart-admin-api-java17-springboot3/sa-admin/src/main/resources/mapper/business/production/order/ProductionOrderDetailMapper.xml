<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.production.order.dao.ProductionOrderDetailDao">

    <select id="queryByOrderId" resultType="net.lab1024.sa.admin.module.business.production.order.domain.vo.OrderDetailVO">
        SELECT detail_id, order_id, color_name, size_name, quantity
        FROM t_production_order_detail
        WHERE order_id = #{orderId} AND deleted_flag = 0
    </select>

    <delete id="deleteByOrderId">
        UPDATE t_production_order_detail SET deleted_flag = 1 WHERE order_id = #{orderId}
    </delete>

</mapper>
